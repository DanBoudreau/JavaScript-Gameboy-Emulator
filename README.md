This project is the creation of a GameBoy emulator using JavaScript and HTML.

JavaScript is a turing-complete Language which allows us to emulate this system through javascript and allows us to build an interpretive emulator.